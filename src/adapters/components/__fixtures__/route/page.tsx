import { Badge } from "../ui/Sample";

// A Next.js app-router route entry: default export, rendered by the router (not by JSX).
export default function HomePage() {
  return (
    <div className="bg-primary">
      <Badge variant="primary" />
    </div>
  );
}
