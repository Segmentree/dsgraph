/** Minimal classic Tailwind config — fixture for the detect path. */
module.exports = {
  theme: {
    extend: {
      colors: {
        brand: { DEFAULT: "#2563eb", 100: "#dbeafe" },
      },
    },
  },
};
